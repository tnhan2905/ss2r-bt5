#include <stdio.h>

int main()
{
    int lenght = 5, width = 10;
    int perimeter = (lenght + width) * 2, area = lenght * width;
    printf("perimeter of rectangle = %d\n", perimeter);
    printf("area of rectangle = %d", area);
    
    return 0;
}
